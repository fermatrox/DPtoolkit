Run linting and type checking on the dp_toolkit package.

1. Run flake8 on dp_toolkit/
2. Run black --check on dp_toolkit/ to verify formatting
3. Run mypy on dp_toolkit/ for type checking

List all issues found, grouped by type (flake8 errors, formatting issues, type errors).
Prioritize by severity and suggest fixes for the most critical issues.
